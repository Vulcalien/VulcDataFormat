# Binary VDF - Changelog

## 2.0
This new format is not compatible with the 1.0 format.\
Now, every element, whatever its type is, can be used as top-level element. Its type code (1 byte) is the first byte of a file.
